// Файл: services/analytics.service.ts
// Логіка отримання даних аналітики з бекенду
/*import { Injectable } from '@nestjs/common';

@Injectable()
export class AnalyticsService {
  async getAnalyticsData() {
    // Приклад реального звернення до бекенду
    const response = await fetch('http://127.0.0.1:5000/analytics');
    if (!response.ok) {
      throw new Error('Помилка при отриманні даних аналітики');
    }
    const data = await response.json();
    return data;
  }
}
*/ export {}
